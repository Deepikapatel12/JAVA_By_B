public class ImportantMethodsOfString {
    public static void main(String[] args) {
        String input = "ramesh Sharma";
        //to get the length of string
        int length = input.length();
        //getting character from string by using charAt method char charAt(int index)
        char firstChar = input.charAt(0);
        //getting index of character from string int indexO(int charcter)method
        //will return the first occurred index if the character is present int the string
        //or else will return -1
int indexOfCharA = input.indexOf('a');

    }
}
